const UsersService = {
    getAllUsers(knex) {
        //getAllUsers
    },
    getUserById(knex, userId) {
        //getUserById
    },
    insertUser(knex, newUser) {
        //create new user
    },
}

module.exports = UsersService;